" Set tab width to 4 characters
set tabstop=4

" when indenting, go over by 4 characters too
set shiftwidth=4

" if we hit <tab>, fill w/spaces instead of \t
set expandtab

" allow us to backspace over "smart" indents
set backspace=indent,eol,start
